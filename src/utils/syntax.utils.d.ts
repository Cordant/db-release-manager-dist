export declare const indentation = "  ";
export declare class SyntaxUtils {
    static snakeCaseToCamelCase(str: string): string;
    static capitalize(str: string): string;
    static camelCaseToTitleCase(str: string): string;
    static simplifyDbFileForAnalysis(fileString: string): string;
}
